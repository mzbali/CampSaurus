<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campgrounds</title>
</head>

<body>
    <h1>Edit Campgrounds</h1>
    <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST">
        <div>
            <label for="campground[title]">Title</label>
            <input type="text" name="campground[title]" id="campground[title]" value="<%= campground.title %>">
        </div>
        <div>
            <label for="campground[price]">Price</label>
            <input type="text" name="campground[price]" id="campground[price]" value="<%= campground.price %>">
        </div>
        <div>
            <label for="campground[location]">Location</label>
            <input type="text" name="campground[location]" id="campground[location]" value="<%= campground.location %>">
        </div>
        <button>Submit</button>
    </form>
    <a href="/campgrounds/<%= campground._id %>">Cancel</a>
</body>

</html>